Pod::Spec.new do |s|
  s.name             = "XXBActionSheet"
  s.version          = "1.0.0"
  s.author       = { "杨小兵" => "six_tiger@163.com" }
  s.platform     = :ios, '7.0'
  s.source_files = "XXBActionSheet"
  s.requires_arc = true
end
